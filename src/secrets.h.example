// WiFi Credentials Template
// Copy this file to secrets.h and fill in your credentials
// secrets.h is gitignored

#ifndef SECRETS_H
#define SECRETS_H

// WiFi credentials (for Proxy mode)
#define WIFI_SSID     "YourWiFiSSID"
#define WIFI_PASSWORD "YourWiFiPassword"

// Proxy node public key (for AP mode)
// This is the MeshCore public key of the node running in PROXY mode
// that will forward WAP requests to the WAPBOX
#define PROXY_NODE_PUBKEY "YourProxyNodePubKey"

#endif // SECRETS_H
